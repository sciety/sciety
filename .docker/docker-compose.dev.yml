version: '3.8'

services:
  app:
    build:
      target: dev
    image: ${IMAGE}:${IMAGE_TAG}-dev
    volumes:
      - '../data:/app/data:ro'
      - '../src:/app/src:ro'
      - '../static:/app/static:ro'
