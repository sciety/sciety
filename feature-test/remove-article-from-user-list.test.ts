describe('when the user removes an article from a generic list', () => {
  it.todo('the article no longer appears on the generic list page');

  describe('on the list card on the user profile page', () => {
    it.todo('the article count is reduced by one');

    it.todo('the last updated date changes');
  });

  describe('on the article card', () => {
    it.todo('on the search results page the list count is decreased by one');
  });
});
