describe('page-tabs', () => {
  describe('when the articles tab is the active tab', () => {
    describe('when linking to the groups tab', () => {
      describe('the evaluatedOnly filter is applied', () => {
        it.todo('maintains the applied state of the filter');
      });

      describe('the evaluatedOnly filter is not applied', () => {
        it.todo('maintains the non-applied state of the filter');
      });
    });
  });

  describe('when the groups tab is the active tab', () => {
    describe('when linking to the articles tab', () => {
      describe('the evaluatedOnly filter is applied', () => {
        it.todo('maintains the applied state of the filter');
      });

      describe('the evaluatedOnly filter is not applied', () => {
        it.todo('maintains the non-applied state of the filter');
      });
    });
  });
});
