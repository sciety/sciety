describe('annotate', () => {
  describe('when the list exists', () => {
    describe('and the article is in the list', () => {
      it.todo('succeeds, raising a new event');
    });

    describe('and the article is not in the list', () => {
      it.todo('fails');
    });
  });

  describe('when the list does not exist', () => {
    it.todo('fails');
  });
});
