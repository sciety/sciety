describe('remove', () => {
  describe('when the list is promoted', () => {
    it.todo('removes the promotion');
  });

  describe('when the list is not promoted', () => {
    it.todo('succeeds, doing nothing');
  });
});
