describe('create', () => {
  describe('given an assignment that does not already exist', () => {
    it.todo('assigns the user as admin of the group');
  });

  describe('given an assignment that already exists', () => {
    it.todo('succeeds and does nothing');
  });
});
