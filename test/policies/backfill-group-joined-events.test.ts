describe('backfill-group-joined-events', () => {
  describe('when given information about an unknown group', () => {
    it.todo('raises a groupJoined event');
  });

  describe('when given information about an known group', () => {
    it.todo('does not raise a groupJoined event');
  });
});
