describe('fetch-review', () => {
  describe('when a service is supported', () => {
    it.todo('returns the fetched evaluation');
  });

  describe('when a service is not supported', () => {
    it.todo('returns not found');
  });
});
