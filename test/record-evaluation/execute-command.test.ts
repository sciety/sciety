describe('execute-command', () => {
  describe('when the command is valid', () => {
    describe('when the evaluation does NOT exist', () => {
      it.todo('returns an EvaluationRecorded event');
    });

    describe('when the evaluation does exist', () => {
      it.todo('returns no events');
    });
  });

  describe('when the article does not exist', () => {
    it.todo('question FOR HANNAH: returns on the left?');
  });

  describe('when the evaluation locator does not resolve', () => {
    it.todo('question FOR HANNAH: returns on the left?');
  });

  describe('when the group does not exist', () => {
    it.todo('returns on the left');
  });
});
