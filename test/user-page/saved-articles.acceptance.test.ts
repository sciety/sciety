describe('saved-articles acceptance', () => {
  describe('article details unavailable for any article', () => {
    it.todo('displays a single error message');
  });

  describe('user has no saved articles', () => {
    it.todo('does not render the component');
  });
});
