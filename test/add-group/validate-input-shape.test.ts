describe('validate-input-shape', () => {
  describe('description path', () => {
    describe('is a markdown file name', () => {
      it.todo('returns on the right');
    });

    describe('contains multiple path segments', () => {
      it.todo('returns on the left');
    });

    describe('does not have a markdown file extension', () => {
      it.todo('returns on the left');
    });
  });
});
