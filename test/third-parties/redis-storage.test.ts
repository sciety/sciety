describe('redis-storage', () => {
  describe('when successfully decoding a storage value', () => {
    it.todo('encodes to the original value');
  });

  describe('when failing to decode a storage value', () => {
    it.todo('returns on the left');
  });
});
