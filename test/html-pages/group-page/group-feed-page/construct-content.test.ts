describe('construct-content', () => {
  describe('when the group has evaluated articles', () => {
    it.todo('contains article cards');
  });

  describe('when the group has no evaluated articles', () => {
    it.todo('contains a no-activity-yet message');
  });
});
