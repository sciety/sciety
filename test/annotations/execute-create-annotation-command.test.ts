describe('execute-create-annotation-command', () => {
  describe('given the target does not have an annotation, when the command is executed', () => {
    it.todo('returns an AnnotationCreated event');
  });
});
