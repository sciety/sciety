describe('augment-with-user-details', () => {
  describe('not all user details are returned from the port', () => {
    it.todo('returns a shorter array of user card view models');
  });

  it.todo('returns the user card view models in the same order as the input followers');
});
