describe('project-response-counts', () => {
  describe('given no events', () => {
    it.todo('returns 0 `yes` and 0 `no`');
  });

  describe('given a single user responded yes event', () => {
    it.todo('returns 1 `yes` and 0 `no`');
  });

  describe('given n user responded yes events from different user', () => {
    it.todo('returns n `yes` and 0 `no`');
  });

  describe('given a single user responded yes event and a single user revoke yes response event', () => {
    it.todo('returns 0 `yes` and 0 `no`');
  });

  describe('given a single user responded: yes, revoke yes, no, revoke no', () => {
    it.todo('returns 0 `yes` and 0 `no`');
  });
});
