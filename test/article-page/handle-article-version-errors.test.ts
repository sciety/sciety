describe('handle-article-version-errors', () => {
  describe('there are article version events', () => {
    it.todo('remains unchanged');
  });

  describe('there are no article version events', () => {
    it.todo('appends an error feed item');
  });
});
