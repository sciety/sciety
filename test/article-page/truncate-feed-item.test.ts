describe('truncate-feed-item', () => {
  describe('content is shorter than 360 characters', () => {
    it.todo('leaves it unchanged');
  });

  describe('content is langer than 360 characters', () => {
    it.todo('breaks it into two parts');

    it.todo('resulting html snippets are valid');
  });
});
