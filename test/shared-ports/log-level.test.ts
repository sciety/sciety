describe('log-level', () => {
  describe('when requested level is debug and the configured level is info', () => {
    it.todo('gets ignored');
  });

  describe('when requested level is error and the configured level is verbose', () => {
    it.todo('does not get ignored');
  });
});
