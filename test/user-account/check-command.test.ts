describe('check-command', () => {
  describe('when the handle in the command is unique', () => {
    it.todo('returns the command');
  });

  describe('when the handle in the command is not unique', () => {
    it.todo('returns an error message');
  });
});
